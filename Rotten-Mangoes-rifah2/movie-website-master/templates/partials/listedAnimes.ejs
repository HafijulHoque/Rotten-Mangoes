<% for (let movie of movies){ %>
    <li>
        <!--<a class="listed" href=<%="/movie/" + movie.id %> >
            <%= movie.title %>
        </a>-->
        <h4> <%= movie.id + ". " + movie.title %></h4>
    </li>
    <% } %>